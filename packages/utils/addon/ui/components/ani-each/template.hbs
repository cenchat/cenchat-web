{{#if key}}
  {{#each items key=key as |item index|}}
    {{yield item index}}
  {{else}}
    {{yield to="inverse"}}
  {{/each}}
{{else}}
  {{#each items as |item index|}}
    {{yield item index}}
  {{else}}
    {{yield to="inverse"}}
  {{/each}}
{{/if}}
