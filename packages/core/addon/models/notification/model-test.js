// import { module, test } from 'qunit';
// import { setupTest } from 'ember-qunit';

// import { setupTestState } from '@cenchat/core/test-support';

// module('Unit | Model | notification', (hooks) => {
//   setupTest(hooks);

//   hooks.beforeEach(async function () {
//     await setupTestState(this);
//   });

//   module('function: getCommentVisitLink', () => {
//     test('should return the page url where the comment belongs', async function (assert) {
//       assert.expect(1);

//       // Arrange
//       const model = await this.store.findRecord('notification', 'notification_b');

//       // Act
//       const result = await model.getCommentVisitLink();

//       // Assert
//       assert.equal(
//         result,
//         'http://site-a.com/foo/bar?cenchat_comment=comment_a',
//       );
//     });
//   });
// });
