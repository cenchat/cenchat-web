import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Model | user-meta-info', function(hooks) {
  setupTest(hooks);

  test('nothing to test', async function(assert) {
    assert.ok(true);
  });
});
