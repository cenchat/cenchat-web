{{#side-nav class="section liquid-fire-side-nav"}}
  {{#ce-top-bar}}
    {{ce-icon-link-button
      href=(href-to "sites.site.pages.page.chats" (qp-for "sites.site.pages.page.chats"))
      title="Navigate back"
      aria-label="Navigate back"
      icon="arrow_back"
    }}
    <h2>Chat</h2>
  {{/ce-top-bar}}

  {{outlet}}

  {{chat-composer
    --session=(lookup "service:session")
    --chat=this.model
    --onSendMessageClick=(action this.handleSendMessageClick)
  }}
{{/side-nav}}
