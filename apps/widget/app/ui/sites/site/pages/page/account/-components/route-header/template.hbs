{{#ce-top-bar class="layout__header"}}
  <h1>Account</h1>
  {{ce-icon-button
    data-test-route-header="sign-out-button"
    title="Sign out"
    aria-label="Sign out"
    icon="exit_to_app"
    --onClick=(action this.handleSignOutClick)
  }}
{{/ce-top-bar}}
