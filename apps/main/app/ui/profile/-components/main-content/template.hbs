<main class="layout__main">
  {{#if (eq --session.model.id --user.id)}}
    {{#if --user.facebookId}}
      {{profile/-components/main-content/follow-suggestion-collection --followingSuggestions=--followingSuggestions}}
    {{/if}}

    {{profile/-components/main-content/following-collection --followings=--followings}}
  {{/if}}

  {{yield}}
</main>
