import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Component | user-collection', function(hooks) {
  setupTest(hooks);

  test('nothing to test so far', function(assert) {
    assert.ok(true);
  });
});
