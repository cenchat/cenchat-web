import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Component | follow-user-button', function(hooks) {
  setupTest(hooks);

  module('function: followUser', function() {
    test('TODO: find a way to test this | should follow user', function(assert) {
      assert.ok(true);
    });
  });
});
