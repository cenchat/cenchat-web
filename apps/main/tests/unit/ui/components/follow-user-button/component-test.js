import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Component | follow-user-button', (hooks) => {
  setupTest(hooks);

  module('function: followUser', () => {
    test('TODO: find a way to test this | should follow user', (assert) => {
      assert.ok(true);
    });
  });
});
