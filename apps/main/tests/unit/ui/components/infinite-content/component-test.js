import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Component | side-nav', function(hooks) {
  setupTest(hooks);

  test('nothing to test so far', function(assert) {
    assert.ok(true);
  });
});
