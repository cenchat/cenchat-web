import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Component | unfollow-user-button', function(hooks) {
  setupTest(hooks);

  module('function: unfollowUser', function() {
    test('TODO: find a way to test this | should unfollow user', function(assert) {
      assert.ok(true);
    });
  });
});
