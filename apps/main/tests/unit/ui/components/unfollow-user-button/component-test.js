import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Component | unfollow-user-button', (hooks) => {
  setupTest(hooks);

  module('function: unfollowUser', () => {
    test('TODO: find a way to test this | should unfollow user', (assert) => {
      assert.ok(true);
    });
  });
});
