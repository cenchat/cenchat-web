import { module, test } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Controller | docs', (hooks) => {
  setupTest(hooks);

  test('nothing to test', async (assert) => {
    assert.expect(1);

    assert.ok(true);
  });
});
